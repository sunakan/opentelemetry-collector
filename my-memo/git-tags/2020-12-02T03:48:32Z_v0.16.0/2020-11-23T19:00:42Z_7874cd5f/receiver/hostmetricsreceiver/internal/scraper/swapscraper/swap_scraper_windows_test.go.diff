diff --git a/receiver/hostmetricsreceiver/internal/scraper/swapscraper/swap_scraper_windows_test.go b/receiver/hostmetricsreceiver/internal/scraper/swapscraper/swap_scraper_windows_test.go
index 565d9c56b..08ddbbcc1 100644
--- a/receiver/hostmetricsreceiver/internal/scraper/swapscraper/swap_scraper_windows_test.go
+++ b/receiver/hostmetricsreceiver/internal/scraper/swapscraper/swap_scraper_windows_test.go
@@ -24,6 +24,7 @@ import (
 	"github.com/stretchr/testify/assert"
 	"github.com/stretchr/testify/require"
 
+	"go.opentelemetry.io/collector/component/componenttest"
 	"go.opentelemetry.io/collector/consumer/consumererror"
 	"go.opentelemetry.io/collector/receiver/hostmetricsreceiver/internal/perfcounters"
 )
@@ -102,10 +103,10 @@ func TestScrape_Errors(t *testing.T) {
 			}
 			scraper.perfCounterScraper = perfcounters.NewMockPerfCounterScraperError(test.scrapeErr, test.getObjectErr, test.getValuesErr)
 
-			err := scraper.Initialize(context.Background())
+			err := scraper.start(context.Background(), componenttest.NewNopHost())
 			require.NoError(t, err, "Failed to initialize swap scraper: %v", err)
 
-			metrics, err := scraper.Scrape(context.Background())
+			metrics, err := scraper.scrape(context.Background())
 			if test.expectedErr != "" {
 				assert.EqualError(t, err, test.expectedErr)
 
