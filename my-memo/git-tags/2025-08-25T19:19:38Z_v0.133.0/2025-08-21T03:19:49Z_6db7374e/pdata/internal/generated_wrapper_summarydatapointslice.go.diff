diff --git a/pdata/internal/generated_wrapper_summarydatapointslice.go b/pdata/internal/generated_wrapper_summarydatapointslice.go
index fd62b0609..2da01f377 100644
--- a/pdata/internal/generated_wrapper_summarydatapointslice.go
+++ b/pdata/internal/generated_wrapper_summarydatapointslice.go
@@ -25,6 +25,7 @@ func CopyOrigSummaryDataPointSlice(dest, src []*otlpmetrics.SummaryDataPoint) []
 		// Cleanup the rest of the elements so GC can free the memory.
 		// This can happen when len(src) < len(dest) < cap(dest).
 		for i := len(src); i < len(dest); i++ {
+			DeleteOrigSummaryDataPoint(dest[i], true)
 			dest[i] = nil
 		}
 		// Add new pointers for missing elements.
