diff --git a/processor/batchprocessor/factory_test.go b/processor/batchprocessor/factory_test.go
index af1d3b966..4a30adedf 100644
--- a/processor/batchprocessor/factory_test.go
+++ b/processor/batchprocessor/factory_test.go
@@ -38,11 +38,11 @@ func TestCreateProcessor(t *testing.T) {
 
 	cfg := factory.CreateDefaultConfig()
 	creationParams := component.ProcessorCreateParams{Logger: zap.NewNop()}
-	tp, err := factory.CreateTraceProcessor(context.Background(), creationParams, nil, cfg)
+	tp, err := factory.CreateTraceProcessor(context.Background(), creationParams, cfg, nil)
 	assert.NotNil(t, tp)
 	assert.NoError(t, err, "cannot create trace processor")
 
-	mp, err := factory.CreateMetricsProcessor(context.Background(), creationParams, nil, cfg)
+	mp, err := factory.CreateMetricsProcessor(context.Background(), creationParams, cfg, nil)
 	assert.NotNil(t, mp)
 	assert.NoError(t, err, "cannot create metric processor")
 
