diff --git a/pdata/internal/generated_proto_span.go b/pdata/internal/generated_proto_span.go
index 4df8e982e..32dbe4f3c 100644
--- a/pdata/internal/generated_proto_span.go
+++ b/pdata/internal/generated_proto_span.go
@@ -12,6 +12,7 @@ import (
 	"sync"
 
 	"go.opentelemetry.io/collector/pdata/internal/json"
+	"go.opentelemetry.io/collector/pdata/internal/metadata"
 	"go.opentelemetry.io/collector/pdata/internal/proto"
 )
 
@@ -45,7 +46,7 @@ var (
 )
 
 func NewSpan() *Span {
-	if !UseProtoPooling.IsEnabled() {
+	if !metadata.PdataUseProtoPoolingFeatureGate.IsEnabled() {
 		return &Span{}
 	}
 	return protoPoolSpan.Get().(*Span)
@@ -56,7 +57,7 @@ func DeleteSpan(orig *Span, nullable bool) {
 		return
 	}
 
-	if !UseProtoPooling.IsEnabled() {
+	if !metadata.PdataUseProtoPoolingFeatureGate.IsEnabled() {
 		orig.Reset()
 		return
 	}
