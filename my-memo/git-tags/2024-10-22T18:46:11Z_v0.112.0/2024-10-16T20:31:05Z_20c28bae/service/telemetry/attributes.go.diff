diff --git a/service/telemetry/attributes.go b/service/telemetry/attributes.go
index 7b1b3b4ed..60b23a345 100644
--- a/service/telemetry/attributes.go
+++ b/service/telemetry/attributes.go
@@ -13,10 +13,8 @@ func attributes(set Settings, cfg Config) map[string]interface{} {
 	for k, v := range cfg.Resource {
 		if v != nil {
 			attrs[k] = *v
-		}
-
-		// the new value is nil, delete the existing key
-		if _, ok := attrs[k]; ok && v == nil {
+		} else {
+			// the new value is nil, delete the existing key
 			delete(attrs, k)
 		}
 	}
