diff --git a/service/settings.go b/service/settings.go
index a634d6489..9b70ac53c 100644
--- a/service/settings.go
+++ b/service/settings.go
@@ -15,7 +15,6 @@
 package service // import "go.opentelemetry.io/collector/service"
 
 import (
-	"go.opentelemetry.io/contrib/zpages"
 	"go.uber.org/zap"
 
 	"go.opentelemetry.io/collector/component"
@@ -36,9 +35,6 @@ type svcSettings struct {
 	// Telemetry represents the service configured telemetry for all the components.
 	Telemetry component.TelemetrySettings
 
-	// ZPagesSpanProcessor represents the SpanProcessor for tracez page.
-	ZPagesSpanProcessor *zpages.SpanProcessor
-
 	// AsyncErrorChannel is the channel that is used to report fatal errors.
 	AsyncErrorChannel chan error
 }
