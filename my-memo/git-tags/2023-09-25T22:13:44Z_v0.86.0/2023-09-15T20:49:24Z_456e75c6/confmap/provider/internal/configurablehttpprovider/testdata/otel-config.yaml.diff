diff --git a/confmap/provider/internal/configurablehttpprovider/testdata/otel-config.yaml b/confmap/provider/internal/configurablehttpprovider/testdata/otel-config.yaml
index 8b7aef3c5..8505ef559 100644
--- a/confmap/provider/internal/configurablehttpprovider/testdata/otel-config.yaml
+++ b/confmap/provider/internal/configurablehttpprovider/testdata/otel-config.yaml
@@ -20,7 +20,7 @@ processors:
     check_interval: 5s
 
 exporters:
-  logging:
+  debug:
     verbosity: detailed
 
 service:
@@ -28,10 +28,10 @@ service:
     traces:
       receivers: [otlp]
       processors: [memory_limiter, batch]
-      exporters: [logging]
+      exporters: [debug]
     metrics:
       receivers: [otlp]
       processors: [memory_limiter, batch]
-      exporters: [logging]
+      exporters: [debug]
 
   extensions: [memory_ballast, zpages]
