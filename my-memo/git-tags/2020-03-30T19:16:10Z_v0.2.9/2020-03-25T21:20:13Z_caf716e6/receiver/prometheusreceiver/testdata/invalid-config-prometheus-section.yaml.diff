diff --git a/receiver/prometheusreceiver/testdata/invalid-config-prometheus-section.yaml b/receiver/prometheusreceiver/testdata/invalid-config-prometheus-section.yaml
new file mode 100644
index 000000000..3f8df1ace
--- /dev/null
+++ b/receiver/prometheusreceiver/testdata/invalid-config-prometheus-section.yaml
@@ -0,0 +1,20 @@
+receivers:
+  prometheus:
+    config:
+      use_start_time_metric: true
+      scrape_configs:
+        - job_name: 'demo'
+          scrape_interval: 5s
+
+processors:
+  exampleprocessor:
+
+exporters:
+  exampleexporter:
+
+service:
+  pipelines:
+    traces:
+      receivers: [prometheus]
+      processors: [exampleprocessor]
+      exporters: [exampleexporter]
