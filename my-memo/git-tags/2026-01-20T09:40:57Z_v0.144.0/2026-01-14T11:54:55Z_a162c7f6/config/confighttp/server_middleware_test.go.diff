diff --git a/config/confighttp/server_middleware_test.go b/config/confighttp/server_middleware_test.go
index f48fae08b..99a2b51d5 100644
--- a/config/confighttp/server_middleware_test.go
+++ b/config/confighttp/server_middleware_test.go
@@ -17,6 +17,7 @@ import (
 	"go.opentelemetry.io/collector/component"
 	"go.opentelemetry.io/collector/component/componenttest"
 	"go.opentelemetry.io/collector/config/configmiddleware"
+	"go.opentelemetry.io/collector/config/confignet"
 	"go.opentelemetry.io/collector/extension"
 	"go.opentelemetry.io/collector/extension/extensionmiddleware"
 	"go.opentelemetry.io/collector/extension/extensionmiddleware/extensionmiddlewaretest"
@@ -91,7 +92,10 @@ func TestServerMiddleware(t *testing.T) {
 		t.Run(tc.name, func(t *testing.T) {
 			// Create server config with the test middlewares
 			cfg := ServerConfig{
-				Endpoint:    "localhost:0",
+				NetAddr: confignet.AddrConfig{
+					Endpoint:  "localhost:0",
+					Transport: confignet.TransportTypeTCP,
+				},
 				Middlewares: tc.middlewares,
 			}
 
@@ -148,7 +152,10 @@ func TestServerMiddlewareErrors(t *testing.T) {
 			name:       "extension_not_found",
 			extensions: map[component.ID]component.Component{},
 			config: ServerConfig{
-				Endpoint: "localhost:0",
+				NetAddr: confignet.AddrConfig{
+					Endpoint:  "localhost:0",
+					Transport: confignet.TransportTypeTCP,
+				},
 				Middlewares: []configmiddleware.Config{
 					{
 						ID: component.MustNewID("nonexistent"),
@@ -163,7 +170,10 @@ func TestServerMiddlewareErrors(t *testing.T) {
 				component.MustNewID("errormw"): extensionmiddlewaretest.NewErr(errors.New("http middleware error")),
 			},
 			config: ServerConfig{
-				Endpoint: "localhost:0",
+				NetAddr: confignet.AddrConfig{
+					Endpoint:  "localhost:0",
+					Transport: confignet.TransportTypeTCP,
+				},
 				Middlewares: []configmiddleware.Config{
 					{
 						ID: component.MustNewID("errormw"),
@@ -199,7 +209,10 @@ func TestServerMiddlewareErrors(t *testing.T) {
 			name:       "grpc_extension_not_found",
 			extensions: map[component.ID]component.Component{},
 			config: ServerConfig{
-				Endpoint: "localhost:0",
+				NetAddr: confignet.AddrConfig{
+					Endpoint:  "localhost:0",
+					Transport: confignet.TransportTypeTCP,
+				},
 				Middlewares: []configmiddleware.Config{
 					{
 						ID: component.MustNewID("nonexistent"),
@@ -214,7 +227,10 @@ func TestServerMiddlewareErrors(t *testing.T) {
 				component.MustNewID("errormw"): extensionmiddlewaretest.NewErr(errors.New("grpc middleware error")),
 			},
 			config: ServerConfig{
-				Endpoint: "localhost:0",
+				NetAddr: confignet.AddrConfig{
+					Endpoint:  "localhost:0",
+					Transport: confignet.TransportTypeTCP,
+				},
 				Middlewares: []configmiddleware.Config{
 					{
 						ID: component.MustNewID("errormw"),
