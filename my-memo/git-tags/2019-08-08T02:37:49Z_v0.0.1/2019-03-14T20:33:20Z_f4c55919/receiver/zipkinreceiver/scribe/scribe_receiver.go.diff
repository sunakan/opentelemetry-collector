diff --git a/receiver/zipkinreceiver/scribe/scribe_receiver.go b/receiver/zipkinreceiver/scribe/scribe_receiver.go
index 51312d9a3..91fcb5863 100644
--- a/receiver/zipkinreceiver/scribe/scribe_receiver.go
+++ b/receiver/zipkinreceiver/scribe/scribe_receiver.go
@@ -174,6 +174,7 @@ func (sc *scribeCollector) Log(messages []*scribe.LogEntry) (r scribe.ResultCode
 
 	tdsSize := 0
 	for _, td := range tds {
+		td.SourceFormat = "zipkin-scribe"
 		sc.nextConsumer.ConsumeTraceData(sc.defaultCtx, td)
 		tdsSize += len(td.Spans)
 	}
