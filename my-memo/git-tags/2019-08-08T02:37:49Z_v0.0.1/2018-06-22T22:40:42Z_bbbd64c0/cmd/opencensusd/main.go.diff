diff --git a/cmd/opencensusd/main.go b/cmd/opencensusd/main.go
index 9739b024b..674018fe4 100644
--- a/cmd/opencensusd/main.go
+++ b/cmd/opencensusd/main.go
@@ -17,6 +17,7 @@
 package main
 
 import (
+	"flag"
 	"fmt"
 	"io"
 	"io/ioutil"
@@ -32,7 +33,10 @@ import (
 )
 
 func main() {
-	ls, err := net.Listen("tcp", "127.0.0.1:")
+	listen := flag.String("listen", "127.0.0.1:", "")
+	flag.Parse()
+
+	ls, err := net.Listen("tcp", *listen)
 	if err != nil {
 		log.Fatalf("Cannot listen: %v", err)
 	}
