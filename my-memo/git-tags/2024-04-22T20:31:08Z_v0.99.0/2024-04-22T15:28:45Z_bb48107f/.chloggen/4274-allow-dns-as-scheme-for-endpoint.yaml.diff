diff --git a/.chloggen/4274-allow-dns-as-scheme-for-endpoint.yaml b/.chloggen/4274-allow-dns-as-scheme-for-endpoint.yaml
new file mode 100644
index 000000000..85668494c
--- /dev/null
+++ b/.chloggen/4274-allow-dns-as-scheme-for-endpoint.yaml
@@ -0,0 +1,13 @@
+# Use this changelog template to create an entry for release notes.
+
+# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
+change_type: bug_fix
+
+# The name of the component, or a single word describing the area of concern, (e.g. otlpreceiver)
+component: exporter/otlp
+
+# A brief description of the change.  Surround your text with quotes ("") if it needs to start with a backtick (`).
+note: Allow DNS scheme to be used in endpoint
+
+# One or more tracking issues or pull requests related to the change
+issues: [4274]
