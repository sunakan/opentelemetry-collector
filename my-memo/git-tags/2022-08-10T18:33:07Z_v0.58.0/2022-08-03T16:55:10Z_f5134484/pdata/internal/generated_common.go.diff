diff --git a/pdata/internal/generated_common.go b/pdata/internal/generated_common.go
index f1155285a..8f72399c3 100644
--- a/pdata/internal/generated_common.go
+++ b/pdata/internal/generated_common.go
@@ -111,10 +111,11 @@ func (es Slice) Len() int {
 // At returns the element at the given index.
 //
 // This function is used mostly for iterating over all the values in the slice:
-//   for i := 0; i < es.Len(); i++ {
-//       e := es.At(i)
-//       ... // Do something with the element
-//   }
+//
+//	for i := 0; i < es.Len(); i++ {
+//	    e := es.At(i)
+//	    ... // Do something with the element
+//	}
 func (es Slice) At(ix int) Value {
 	return newValue(&(*es.orig)[ix])
 }
@@ -139,12 +140,13 @@ func (es Slice) CopyTo(dest Slice) {
 // 2. If the newCap > cap then the slice capacity will be expanded to equal newCap.
 //
 // Here is how a new Slice can be initialized:
-//   es := NewSlice()
-//   es.EnsureCapacity(4)
-//   for i := 0; i < 4; i++ {
-//       e := es.AppendEmpty()
-//       // Here should set all the values for e.
-//   }
+//
+//	es := NewSlice()
+//	es.EnsureCapacity(4)
+//	for i := 0; i < 4; i++ {
+//	    e := es.AppendEmpty()
+//	    // Here should set all the values for e.
+//	}
 func (es Slice) EnsureCapacity(newCap int) {
 	oldCap := cap(*es.orig)
 	if newCap <= oldCap {
