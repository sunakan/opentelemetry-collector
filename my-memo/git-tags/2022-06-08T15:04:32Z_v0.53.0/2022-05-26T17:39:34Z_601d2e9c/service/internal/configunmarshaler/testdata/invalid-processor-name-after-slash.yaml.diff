diff --git a/service/internal/configunmarshaler/testdata/invalid-processor-name-after-slash.yaml b/service/internal/configunmarshaler/testdata/invalid-processor-name-after-slash.yaml
index 7b0fe077c..d67a44c03 100644
--- a/service/internal/configunmarshaler/testdata/invalid-processor-name-after-slash.yaml
+++ b/service/internal/configunmarshaler/testdata/invalid-processor-name-after-slash.yaml
@@ -1,13 +1,13 @@
 receivers:
-  examplereceiver:
+  nop:
 exporters:
-  exampleexporter:
+  nop:
 processors:
-  exampleprocessor:
-  exampleprocessor/:
+  nop:
+  nop/:
 service:
   pipelines:
     traces:
-      receivers: [examplereceiver]
-      processors: [exampleprocessor]
-      exporters: [exampleexporter]
+      receivers: [nop]
+      processors: [nop]
+      exporters: [nop]
