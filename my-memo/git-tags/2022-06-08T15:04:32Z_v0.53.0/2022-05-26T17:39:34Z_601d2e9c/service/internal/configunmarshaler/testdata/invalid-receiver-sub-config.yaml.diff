diff --git a/service/internal/configunmarshaler/testdata/invalid-receiver-sub-config.yaml b/service/internal/configunmarshaler/testdata/invalid-receiver-sub-config.yaml
index 2fb171b21..99deefefd 100644
--- a/service/internal/configunmarshaler/testdata/invalid-receiver-sub-config.yaml
+++ b/service/internal/configunmarshaler/testdata/invalid-receiver-sub-config.yaml
@@ -1,13 +1,13 @@
 receivers:
-  examplereceiver:
+  nop:
     tests
 exporters:
-  exampleexporter:
+  nop:
 processors:
-  exampleprocessor:
+  nop:
 service:
   pipelines:
     traces:
-      receivers: [examplereceiver]
-      exporters: [exampleexporter]
-      processors: [exampleprocessor]
+      receivers: [nop]
+      exporters: [nop]
+      processors: [nop]
