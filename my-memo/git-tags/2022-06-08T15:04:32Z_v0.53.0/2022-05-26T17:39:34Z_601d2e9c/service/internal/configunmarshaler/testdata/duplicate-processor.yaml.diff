diff --git a/service/internal/configunmarshaler/testdata/duplicate-processor.yaml b/service/internal/configunmarshaler/testdata/duplicate-processor.yaml
index 0871b540f..6247c1d4c 100644
--- a/service/internal/configunmarshaler/testdata/duplicate-processor.yaml
+++ b/service/internal/configunmarshaler/testdata/duplicate-processor.yaml
@@ -1,13 +1,13 @@
 receivers:
-  examplereceiver:
+  nop:
 exporters:
-  exampleexporter:
+  nop:
 processors:
-  exampleprocessor/ proc:
-  exampleprocessor /proc :
+  nop/ proc:
+  nop /proc :
 service:
   pipelines:
     traces:
-      receivers: [examplereceiver]
-      exporters: [exampleexporter]
-      processors: [exampleprocessor]
+      receivers: [nop]
+      exporters: [nop]
+      processors: [nop]
