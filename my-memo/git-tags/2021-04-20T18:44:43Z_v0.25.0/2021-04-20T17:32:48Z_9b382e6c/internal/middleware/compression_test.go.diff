diff --git a/internal/middleware/compression_test.go b/internal/middleware/compression_test.go
index a30385167..b49cdb1ef 100644
--- a/internal/middleware/compression_test.go
+++ b/internal/middleware/compression_test.go
@@ -85,7 +85,8 @@ func TestHTTPClientCompression(t *testing.T) {
 			res, err := client.Do(req)
 			require.NoError(t, err)
 
-			ioutil.ReadAll(res.Body)
+			_, err = ioutil.ReadAll(res.Body)
+			require.NoError(t, err)
 			require.NoError(t, res.Body.Close(), "failed to close request body: %v", err)
 			require.NoError(t, srv.Close())
 		})
