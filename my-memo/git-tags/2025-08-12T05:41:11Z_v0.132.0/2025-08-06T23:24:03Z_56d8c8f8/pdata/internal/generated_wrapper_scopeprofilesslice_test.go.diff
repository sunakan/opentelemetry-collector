diff --git a/pdata/internal/generated_wrapper_scopeprofilesslice_test.go b/pdata/internal/generated_wrapper_scopeprofilesslice_test.go
index b4bf5ac3c..853a2507b 100644
--- a/pdata/internal/generated_wrapper_scopeprofilesslice_test.go
+++ b/pdata/internal/generated_wrapper_scopeprofilesslice_test.go
@@ -10,8 +10,10 @@ import (
 	"testing"
 
 	"github.com/stretchr/testify/assert"
+	"github.com/stretchr/testify/require"
 
 	otlpprofiles "go.opentelemetry.io/collector/pdata/internal/data/protogen/profiles/v1development"
+	"go.opentelemetry.io/collector/pdata/internal/json"
 )
 
 func TestCopyOrigScopeProfilesSlice(t *testing.T) {
@@ -38,3 +40,19 @@ func TestCopyOrigScopeProfilesSlice(t *testing.T) {
 	dest = CopyOrigScopeProfilesSlice(dest, src)
 	assert.Equal(t, GenerateOrigTestScopeProfilesSlice(), dest)
 }
+
+func TestMarshalAndUnmarshalJSONOrigScopeProfilesSlice(t *testing.T) {
+	src := GenerateOrigTestScopeProfilesSlice()
+
+	stream := json.BorrowStream(nil)
+	defer json.ReturnStream(stream)
+	MarshalJSONOrigScopeProfilesSlice(src, stream)
+	require.NoError(t, stream.Error())
+
+	iter := json.BorrowIterator(stream.Buffer())
+	defer json.ReturnIterator(iter)
+	dest := UnmarshalJSONOrigScopeProfilesSlice(iter)
+	require.NoError(t, iter.Error())
+
+	assert.Equal(t, src, dest)
+}
