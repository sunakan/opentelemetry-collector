diff --git a/pdata/internal/generated_wrapper_anyvalueslice_test.go b/pdata/internal/generated_wrapper_anyvalueslice_test.go
index 387585a28..cd5434825 100644
--- a/pdata/internal/generated_wrapper_anyvalueslice_test.go
+++ b/pdata/internal/generated_wrapper_anyvalueslice_test.go
@@ -10,8 +10,10 @@ import (
 	"testing"
 
 	"github.com/stretchr/testify/assert"
+	"github.com/stretchr/testify/require"
 
 	otlpcommon "go.opentelemetry.io/collector/pdata/internal/data/protogen/common/v1"
+	"go.opentelemetry.io/collector/pdata/internal/json"
 )
 
 func TestCopyOrigAnyValueSlice(t *testing.T) {
@@ -38,3 +40,19 @@ func TestCopyOrigAnyValueSlice(t *testing.T) {
 	dest = CopyOrigAnyValueSlice(dest, src)
 	assert.Equal(t, GenerateOrigTestAnyValueSlice(), dest)
 }
+
+func TestMarshalAndUnmarshalJSONOrigAnyValueSlice(t *testing.T) {
+	src := GenerateOrigTestAnyValueSlice()
+
+	stream := json.BorrowStream(nil)
+	defer json.ReturnStream(stream)
+	MarshalJSONOrigAnyValueSlice(src, stream)
+	require.NoError(t, stream.Error())
+
+	iter := json.BorrowIterator(stream.Buffer())
+	defer json.ReturnIterator(iter)
+	dest := UnmarshalJSONOrigAnyValueSlice(iter)
+	require.NoError(t, iter.Error())
+
+	assert.Equal(t, src, dest)
+}
