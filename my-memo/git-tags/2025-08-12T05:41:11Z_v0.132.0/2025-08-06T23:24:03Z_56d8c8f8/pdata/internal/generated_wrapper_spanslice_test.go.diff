diff --git a/pdata/internal/generated_wrapper_spanslice_test.go b/pdata/internal/generated_wrapper_spanslice_test.go
index f551694fc..db44b34a8 100644
--- a/pdata/internal/generated_wrapper_spanslice_test.go
+++ b/pdata/internal/generated_wrapper_spanslice_test.go
@@ -10,8 +10,10 @@ import (
 	"testing"
 
 	"github.com/stretchr/testify/assert"
+	"github.com/stretchr/testify/require"
 
 	otlptrace "go.opentelemetry.io/collector/pdata/internal/data/protogen/trace/v1"
+	"go.opentelemetry.io/collector/pdata/internal/json"
 )
 
 func TestCopyOrigSpanSlice(t *testing.T) {
@@ -38,3 +40,19 @@ func TestCopyOrigSpanSlice(t *testing.T) {
 	dest = CopyOrigSpanSlice(dest, src)
 	assert.Equal(t, GenerateOrigTestSpanSlice(), dest)
 }
+
+func TestMarshalAndUnmarshalJSONOrigSpanSlice(t *testing.T) {
+	src := GenerateOrigTestSpanSlice()
+
+	stream := json.BorrowStream(nil)
+	defer json.ReturnStream(stream)
+	MarshalJSONOrigSpanSlice(src, stream)
+	require.NoError(t, stream.Error())
+
+	iter := json.BorrowIterator(stream.Buffer())
+	defer json.ReturnIterator(iter)
+	dest := UnmarshalJSONOrigSpanSlice(iter)
+	require.NoError(t, iter.Error())
+
+	assert.Equal(t, src, dest)
+}
