diff --git a/pdata/internal/generated_wrapper_uint64slice_test.go b/pdata/internal/generated_wrapper_uint64slice_test.go
index f3a722e1b..997fa52b0 100644
--- a/pdata/internal/generated_wrapper_uint64slice_test.go
+++ b/pdata/internal/generated_wrapper_uint64slice_test.go
@@ -10,6 +10,9 @@ import (
 	"testing"
 
 	"github.com/stretchr/testify/assert"
+	"github.com/stretchr/testify/require"
+
+	"go.opentelemetry.io/collector/pdata/internal/json"
 )
 
 func TestCopyOrigUint64Slice(t *testing.T) {
@@ -22,3 +25,19 @@ func TestCopyOrigUint64Slice(t *testing.T) {
 	dest = CopyOrigUint64Slice(dest, src)
 	assert.Equal(t, src, dest)
 }
+
+func TestMarshalAndUnmarshalJSONOrigUint64Slice(t *testing.T) {
+	src := GenerateOrigTestUint64Slice()
+
+	stream := json.BorrowStream(nil)
+	defer json.ReturnStream(stream)
+	MarshalJSONOrigUint64Slice(src, stream)
+	require.NoError(t, stream.Error())
+
+	iter := json.BorrowIterator(stream.Buffer())
+	defer json.ReturnIterator(iter)
+	dest := UnmarshalJSONOrigUint64Slice(iter)
+	require.NoError(t, iter.Error())
+
+	assert.Equal(t, src, dest)
+}
