diff --git a/pdata/internal/generated_wrapper_span_eventslice_test.go b/pdata/internal/generated_wrapper_span_eventslice_test.go
index 2af8b0d26..052b851a4 100644
--- a/pdata/internal/generated_wrapper_span_eventslice_test.go
+++ b/pdata/internal/generated_wrapper_span_eventslice_test.go
@@ -10,8 +10,10 @@ import (
 	"testing"
 
 	"github.com/stretchr/testify/assert"
+	"github.com/stretchr/testify/require"
 
 	otlptrace "go.opentelemetry.io/collector/pdata/internal/data/protogen/trace/v1"
+	"go.opentelemetry.io/collector/pdata/internal/json"
 )
 
 func TestCopyOrigSpan_EventSlice(t *testing.T) {
@@ -38,3 +40,19 @@ func TestCopyOrigSpan_EventSlice(t *testing.T) {
 	dest = CopyOrigSpan_EventSlice(dest, src)
 	assert.Equal(t, GenerateOrigTestSpan_EventSlice(), dest)
 }
+
+func TestMarshalAndUnmarshalJSONOrigSpan_EventSlice(t *testing.T) {
+	src := GenerateOrigTestSpan_EventSlice()
+
+	stream := json.BorrowStream(nil)
+	defer json.ReturnStream(stream)
+	MarshalJSONOrigSpan_EventSlice(src, stream)
+	require.NoError(t, stream.Error())
+
+	iter := json.BorrowIterator(stream.Buffer())
+	defer json.ReturnIterator(iter)
+	dest := UnmarshalJSONOrigSpan_EventSlice(iter)
+	require.NoError(t, iter.Error())
+
+	assert.Equal(t, src, dest)
+}
