diff --git a/pdata/internal/generated_wrapper_profileslice_test.go b/pdata/internal/generated_wrapper_profileslice_test.go
index 773c597e8..205e73230 100644
--- a/pdata/internal/generated_wrapper_profileslice_test.go
+++ b/pdata/internal/generated_wrapper_profileslice_test.go
@@ -10,8 +10,10 @@ import (
 	"testing"
 
 	"github.com/stretchr/testify/assert"
+	"github.com/stretchr/testify/require"
 
 	otlpprofiles "go.opentelemetry.io/collector/pdata/internal/data/protogen/profiles/v1development"
+	"go.opentelemetry.io/collector/pdata/internal/json"
 )
 
 func TestCopyOrigProfileSlice(t *testing.T) {
@@ -38,3 +40,19 @@ func TestCopyOrigProfileSlice(t *testing.T) {
 	dest = CopyOrigProfileSlice(dest, src)
 	assert.Equal(t, GenerateOrigTestProfileSlice(), dest)
 }
+
+func TestMarshalAndUnmarshalJSONOrigProfileSlice(t *testing.T) {
+	src := GenerateOrigTestProfileSlice()
+
+	stream := json.BorrowStream(nil)
+	defer json.ReturnStream(stream)
+	MarshalJSONOrigProfileSlice(src, stream)
+	require.NoError(t, stream.Error())
+
+	iter := json.BorrowIterator(stream.Buffer())
+	defer json.ReturnIterator(iter)
+	dest := UnmarshalJSONOrigProfileSlice(iter)
+	require.NoError(t, iter.Error())
+
+	assert.Equal(t, src, dest)
+}
