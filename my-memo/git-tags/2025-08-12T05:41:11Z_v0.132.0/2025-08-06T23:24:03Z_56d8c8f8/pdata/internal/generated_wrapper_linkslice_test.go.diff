diff --git a/pdata/internal/generated_wrapper_linkslice_test.go b/pdata/internal/generated_wrapper_linkslice_test.go
index f16e2944f..c77fd71c2 100644
--- a/pdata/internal/generated_wrapper_linkslice_test.go
+++ b/pdata/internal/generated_wrapper_linkslice_test.go
@@ -10,8 +10,10 @@ import (
 	"testing"
 
 	"github.com/stretchr/testify/assert"
+	"github.com/stretchr/testify/require"
 
 	otlpprofiles "go.opentelemetry.io/collector/pdata/internal/data/protogen/profiles/v1development"
+	"go.opentelemetry.io/collector/pdata/internal/json"
 )
 
 func TestCopyOrigLinkSlice(t *testing.T) {
@@ -38,3 +40,19 @@ func TestCopyOrigLinkSlice(t *testing.T) {
 	dest = CopyOrigLinkSlice(dest, src)
 	assert.Equal(t, GenerateOrigTestLinkSlice(), dest)
 }
+
+func TestMarshalAndUnmarshalJSONOrigLinkSlice(t *testing.T) {
+	src := GenerateOrigTestLinkSlice()
+
+	stream := json.BorrowStream(nil)
+	defer json.ReturnStream(stream)
+	MarshalJSONOrigLinkSlice(src, stream)
+	require.NoError(t, stream.Error())
+
+	iter := json.BorrowIterator(stream.Buffer())
+	defer json.ReturnIterator(iter)
+	dest := UnmarshalJSONOrigLinkSlice(iter)
+	require.NoError(t, iter.Error())
+
+	assert.Equal(t, src, dest)
+}
