diff --git a/pdata/internal/generated_wrapper_mappingslice_test.go b/pdata/internal/generated_wrapper_mappingslice_test.go
index 78ff89d09..a73963a34 100644
--- a/pdata/internal/generated_wrapper_mappingslice_test.go
+++ b/pdata/internal/generated_wrapper_mappingslice_test.go
@@ -10,8 +10,10 @@ import (
 	"testing"
 
 	"github.com/stretchr/testify/assert"
+	"github.com/stretchr/testify/require"
 
 	otlpprofiles "go.opentelemetry.io/collector/pdata/internal/data/protogen/profiles/v1development"
+	"go.opentelemetry.io/collector/pdata/internal/json"
 )
 
 func TestCopyOrigMappingSlice(t *testing.T) {
@@ -38,3 +40,19 @@ func TestCopyOrigMappingSlice(t *testing.T) {
 	dest = CopyOrigMappingSlice(dest, src)
 	assert.Equal(t, GenerateOrigTestMappingSlice(), dest)
 }
+
+func TestMarshalAndUnmarshalJSONOrigMappingSlice(t *testing.T) {
+	src := GenerateOrigTestMappingSlice()
+
+	stream := json.BorrowStream(nil)
+	defer json.ReturnStream(stream)
+	MarshalJSONOrigMappingSlice(src, stream)
+	require.NoError(t, stream.Error())
+
+	iter := json.BorrowIterator(stream.Buffer())
+	defer json.ReturnIterator(iter)
+	dest := UnmarshalJSONOrigMappingSlice(iter)
+	require.NoError(t, iter.Error())
+
+	assert.Equal(t, src, dest)
+}
