diff --git a/pdata/internal/generated_wrapper_scopespansslice_test.go b/pdata/internal/generated_wrapper_scopespansslice_test.go
index d4075ca7d..f9d791ed7 100644
--- a/pdata/internal/generated_wrapper_scopespansslice_test.go
+++ b/pdata/internal/generated_wrapper_scopespansslice_test.go
@@ -10,8 +10,10 @@ import (
 	"testing"
 
 	"github.com/stretchr/testify/assert"
+	"github.com/stretchr/testify/require"
 
 	otlptrace "go.opentelemetry.io/collector/pdata/internal/data/protogen/trace/v1"
+	"go.opentelemetry.io/collector/pdata/internal/json"
 )
 
 func TestCopyOrigScopeSpansSlice(t *testing.T) {
@@ -38,3 +40,19 @@ func TestCopyOrigScopeSpansSlice(t *testing.T) {
 	dest = CopyOrigScopeSpansSlice(dest, src)
 	assert.Equal(t, GenerateOrigTestScopeSpansSlice(), dest)
 }
+
+func TestMarshalAndUnmarshalJSONOrigScopeSpansSlice(t *testing.T) {
+	src := GenerateOrigTestScopeSpansSlice()
+
+	stream := json.BorrowStream(nil)
+	defer json.ReturnStream(stream)
+	MarshalJSONOrigScopeSpansSlice(src, stream)
+	require.NoError(t, stream.Error())
+
+	iter := json.BorrowIterator(stream.Buffer())
+	defer json.ReturnIterator(iter)
+	dest := UnmarshalJSONOrigScopeSpansSlice(iter)
+	require.NoError(t, iter.Error())
+
+	assert.Equal(t, src, dest)
+}
