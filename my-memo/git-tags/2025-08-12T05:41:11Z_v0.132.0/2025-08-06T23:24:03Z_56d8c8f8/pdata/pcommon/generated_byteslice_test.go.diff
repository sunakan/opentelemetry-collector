diff --git a/pdata/pcommon/generated_byteslice_test.go b/pdata/pcommon/generated_byteslice_test.go
index 279adcabc..71bdc834c 100644
--- a/pdata/pcommon/generated_byteslice_test.go
+++ b/pdata/pcommon/generated_byteslice_test.go
@@ -10,10 +10,8 @@ import (
 	"testing"
 
 	"github.com/stretchr/testify/assert"
-	"github.com/stretchr/testify/require"
 
 	"go.opentelemetry.io/collector/pdata/internal"
-	"go.opentelemetry.io/collector/pdata/internal/json"
 )
 
 func TestNewByteSlice(t *testing.T) {
@@ -131,22 +129,6 @@ func TestByteSliceEqual(t *testing.T) {
 	assert.True(t, ms.Equal(ms2))
 }
 
-func TestByteSlice_MarshalAndUnmarshalJSON(t *testing.T) {
-	stream := json.BorrowStream(nil)
-	defer json.ReturnStream(stream)
-	src := NewByteSlice()
-	*src.getOrig() = internal.GenerateOrigTestByteSlice()
-	internal.MarshalJSONStreamByteSlice(internal.ByteSlice(src), stream)
-	require.NoError(t, stream.Error())
-
-	iter := json.BorrowIterator(stream.Buffer())
-	defer json.ReturnIterator(iter)
-	dest := NewByteSlice()
-	internal.UnmarshalJSONIterByteSlice(internal.ByteSlice(dest), iter)
-
-	assert.Equal(t, src, dest)
-}
-
 func BenchmarkByteSliceEqual(b *testing.B) {
 	ms := NewByteSlice()
 	ms.Append(1, 2, 3)
