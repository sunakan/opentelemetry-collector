diff --git a/pdata/pprofile/generated_profilesslice.go b/pdata/pprofile/generated_profilesslice.go
index 2410dfa56..9c6e7f5ff 100644
--- a/pdata/pprofile/generated_profilesslice.go
+++ b/pdata/pprofile/generated_profilesslice.go
@@ -180,16 +180,30 @@ func (ms ProfilesSlice) unmarshalJSONIter(iter *json.Iterator) {
 }
 
 func copyOrigProfilesSlice(dest, src []*otlpprofiles.Profile) []*otlpprofiles.Profile {
+	var newDest []*otlpprofiles.Profile
 	if cap(dest) < len(src) {
-		dest = make([]*otlpprofiles.Profile, len(src))
-		data := make([]otlpprofiles.Profile, len(src))
-		for i := range src {
-			dest[i] = &data[i]
+		newDest = make([]*otlpprofiles.Profile, len(src))
+		// Copy old pointers to re-use.
+		copy(newDest, dest)
+		// Add new pointers for missing elements from len(dest) to len(srt).
+		for i := len(dest); i < len(src); i++ {
+			newDest[i] = &otlpprofiles.Profile{}
+		}
+	} else {
+		newDest = dest[:len(src)]
+		// Cleanup the rest of the elements so GC can free the memory.
+		// This can happen when len(src) < len(dest) < cap(dest).
+		for i := len(src); i < len(dest); i++ {
+			dest[i] = nil
+		}
+		// Add new pointers for missing elements.
+		// This can happen when len(dest) < len(src) < cap(dest).
+		for i := len(dest); i < len(src); i++ {
+			newDest[i] = &otlpprofiles.Profile{}
 		}
 	}
-	dest = dest[:len(src)]
 	for i := range src {
-		copyOrigProfile(dest[i], src[i])
+		copyOrigProfile(newDest[i], src[i])
 	}
-	return dest
+	return newDest
 }
