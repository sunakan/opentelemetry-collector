diff --git a/pdata/internal/generated_wrapper_entityref.go b/pdata/internal/generated_wrapper_entityref.go
index def6f4460..8f4d67e14 100644
--- a/pdata/internal/generated_wrapper_entityref.go
+++ b/pdata/internal/generated_wrapper_entityref.go
@@ -28,13 +28,6 @@ func NewEntityRef(orig *otlpcommon.EntityRef, state *State) EntityRef {
 	return EntityRef{orig: orig, state: state}
 }
 
-func CopyOrigEntityRef(dest, src *otlpcommon.EntityRef) {
-	dest.SchemaUrl = src.SchemaUrl
-	dest.Type = src.Type
-	dest.IdKeys = CopyOrigStringSlice(dest.IdKeys, src.IdKeys)
-	dest.DescriptionKeys = CopyOrigStringSlice(dest.DescriptionKeys, src.DescriptionKeys)
-}
-
 func GenerateTestEntityRef() EntityRef {
 	orig := otlpcommon.EntityRef{}
 	state := StateMutable
@@ -90,3 +83,10 @@ func UnmarshalJSONIterEntityRef(ms EntityRef, iter *json.Iterator) {
 		return true
 	})
 }
+
+func CopyOrigEntityRef(dest, src *otlpcommon.EntityRef) {
+	dest.SchemaUrl = src.SchemaUrl
+	dest.Type = src.Type
+	dest.IdKeys = CopyOrigStringSlice(dest.IdKeys, src.IdKeys)
+	dest.DescriptionKeys = CopyOrigStringSlice(dest.DescriptionKeys, src.DescriptionKeys)
+}
