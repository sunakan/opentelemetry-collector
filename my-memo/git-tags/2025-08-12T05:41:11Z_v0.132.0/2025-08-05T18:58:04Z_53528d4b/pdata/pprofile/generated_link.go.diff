diff --git a/pdata/pprofile/generated_link.go b/pdata/pprofile/generated_link.go
index d3d68735f..4e3637afa 100644
--- a/pdata/pprofile/generated_link.go
+++ b/pdata/pprofile/generated_link.go
@@ -77,7 +77,7 @@ func (ms Link) SetSpanID(v pcommon.SpanID) {
 // CopyTo copies all properties from the current struct overriding the destination.
 func (ms Link) CopyTo(dest Link) {
 	dest.state.AssertMutable()
-	copyOrigLink(dest.orig, ms.orig)
+	internal.CopyOrigLink(dest.orig, ms.orig)
 }
 
 // marshalJSONStream marshals all properties from the current struct to the destination stream.
@@ -108,8 +108,3 @@ func (ms Link) unmarshalJSONIter(iter *json.Iterator) {
 		return true
 	})
 }
-
-func copyOrigLink(dest, src *otlpprofiles.Link) {
-	dest.TraceId = src.TraceId
-	dest.SpanId = src.SpanId
-}
