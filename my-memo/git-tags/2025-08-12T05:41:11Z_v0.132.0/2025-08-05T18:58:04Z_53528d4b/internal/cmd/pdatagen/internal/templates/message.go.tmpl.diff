diff --git a/internal/cmd/pdatagen/internal/templates/message.go.tmpl b/internal/cmd/pdatagen/internal/templates/message.go.tmpl
index 3171dd248..5642ed68e 100644
--- a/internal/cmd/pdatagen/internal/templates/message.go.tmpl
+++ b/internal/cmd/pdatagen/internal/templates/message.go.tmpl
@@ -23,12 +23,12 @@ import (
 type {{ .structName }} internal.{{ .structName }}
 {{- else }}
 type {{ .structName }} struct {
-	orig *{{ .originName }}
+	orig *{{ .originFullName }}
 	state *internal.State
 }
 {{- end }}
 
-func new{{ .structName }}(orig *{{ .originName }}, state *internal.State) {{ .structName }} {
+func new{{ .structName }}(orig *{{ .originFullName }}, state *internal.State) {{ .structName }} {
 	{{- if .isCommon }}
 	return {{ .structName }}(internal.New{{ .structName }}(orig, state))
 	{{- else }}
@@ -42,7 +42,7 @@ func new{{ .structName }}(orig *{{ .originName }}, state *internal.State) {{ .st
 // OR directly access the member if this is embedded in another struct.
 func New{{ .structName }}() {{ .structName }} {
 	state := internal.StateMutable
-	return new{{ .structName }}(&{{ .originName }}{}, &state)
+	return new{{ .structName }}(&{{ .originFullName }}{}, &state)
 }
 
 // MoveTo moves all properties from the current struct overriding the destination and
@@ -55,7 +55,7 @@ func (ms {{ .structName }}) MoveTo(dest {{ .structName }}) {
 		return
 	}
 	*dest.{{ .origAccessor }} = *ms.{{ .origAccessor }}
-	*ms.{{ .origAccessor }} = {{ .originName }}{}
+	*ms.{{ .origAccessor }} = {{ .originFullName }}{}
 }
 
 {{ range .fields -}}
@@ -65,15 +65,11 @@ func (ms {{ .structName }}) MoveTo(dest {{ .structName }}) {
 // CopyTo copies all properties from the current struct overriding the destination.
 func (ms {{ .structName }}) CopyTo(dest {{ .structName }}) {
 	dest.{{ .stateAccessor }}.AssertMutable()
-    {{- if .isCommon }}
-    internal.CopyOrig{{ .structName }}(dest.{{ .origAccessor }}, ms.{{ .origAccessor }})
-    {{- else }}
-    copyOrig{{ .structName }}(dest.{{ .origAccessor }}, ms.{{ .origAccessor }})
-	{{- end }}
+    internal.CopyOrig{{ .originName }}(dest.{{ .origAccessor }}, ms.{{ .origAccessor }})
 }
 
 {{ if .isCommon -}}
-func (ms {{ .structName }}) getOrig() *{{ .originName }} {
+func (ms {{ .structName }}) getOrig() *{{ .originFullName }} {
 	return internal.GetOrig{{ .structName }}(internal.{{ .structName }}(ms))
 }
 
@@ -104,9 +100,4 @@ func (ms {{ .structName }}) unmarshalJSONIter(iter *json.Iterator) {
 	})
 }
 
-func copyOrig{{ .structName }}(dest, src *{{ .originName }}) {
-	{{- range .fields }}
-	{{ .GenerateCopyOrig $.messageStruct }}
-	{{- end }}
-}
 {{- end }}
