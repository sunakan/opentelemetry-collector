diff --git a/pdata/internal/generated_wrapper_histogramdatapointslice_test.go b/pdata/internal/generated_wrapper_histogramdatapointslice_test.go
index 42f8bf440..8efaf6efa 100644
--- a/pdata/internal/generated_wrapper_histogramdatapointslice_test.go
+++ b/pdata/internal/generated_wrapper_histogramdatapointslice_test.go
@@ -10,10 +10,8 @@ import (
 	"testing"
 
 	"github.com/stretchr/testify/assert"
-	"github.com/stretchr/testify/require"
 
 	otlpmetrics "go.opentelemetry.io/collector/pdata/internal/data/protogen/metrics/v1"
-	"go.opentelemetry.io/collector/pdata/internal/json"
 )
 
 func TestCopyOrigHistogramDataPointSlice(t *testing.T) {
@@ -40,19 +38,3 @@ func TestCopyOrigHistogramDataPointSlice(t *testing.T) {
 	dest = CopyOrigHistogramDataPointSlice(dest, src)
 	assert.Equal(t, GenerateOrigTestHistogramDataPointSlice(), dest)
 }
-
-func TestMarshalAndUnmarshalJSONOrigHistogramDataPointSlice(t *testing.T) {
-	src := GenerateOrigTestHistogramDataPointSlice()
-
-	stream := json.BorrowStream(nil)
-	defer json.ReturnStream(stream)
-	MarshalJSONOrigHistogramDataPointSlice(src, stream)
-	require.NoError(t, stream.Error())
-
-	iter := json.BorrowIterator(stream.Buffer())
-	defer json.ReturnIterator(iter)
-	dest := UnmarshalJSONOrigHistogramDataPointSlice(iter)
-	require.NoError(t, iter.Error())
-
-	assert.Equal(t, src, dest)
-}
