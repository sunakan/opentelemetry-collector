diff --git a/pdata/internal/generated_wrapper_valuetypeslice_test.go b/pdata/internal/generated_wrapper_valuetypeslice_test.go
index 7fd33d1e3..292954a35 100644
--- a/pdata/internal/generated_wrapper_valuetypeslice_test.go
+++ b/pdata/internal/generated_wrapper_valuetypeslice_test.go
@@ -10,10 +10,8 @@ import (
 	"testing"
 
 	"github.com/stretchr/testify/assert"
-	"github.com/stretchr/testify/require"
 
 	otlpprofiles "go.opentelemetry.io/collector/pdata/internal/data/protogen/profiles/v1development"
-	"go.opentelemetry.io/collector/pdata/internal/json"
 )
 
 func TestCopyOrigValueTypeSlice(t *testing.T) {
@@ -40,19 +38,3 @@ func TestCopyOrigValueTypeSlice(t *testing.T) {
 	dest = CopyOrigValueTypeSlice(dest, src)
 	assert.Equal(t, GenerateOrigTestValueTypeSlice(), dest)
 }
-
-func TestMarshalAndUnmarshalJSONOrigValueTypeSlice(t *testing.T) {
-	src := GenerateOrigTestValueTypeSlice()
-
-	stream := json.BorrowStream(nil)
-	defer json.ReturnStream(stream)
-	MarshalJSONOrigValueTypeSlice(src, stream)
-	require.NoError(t, stream.Error())
-
-	iter := json.BorrowIterator(stream.Buffer())
-	defer json.ReturnIterator(iter)
-	dest := UnmarshalJSONOrigValueTypeSlice(iter)
-	require.NoError(t, iter.Error())
-
-	assert.Equal(t, src, dest)
-}
