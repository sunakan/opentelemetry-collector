diff --git a/pdata/xpdata/entity/generated_entityrefslice_test.go b/pdata/xpdata/entity/generated_entityrefslice_test.go
index 735a1a14e..8f43ceb32 100644
--- a/pdata/xpdata/entity/generated_entityrefslice_test.go
+++ b/pdata/xpdata/entity/generated_entityrefslice_test.go
@@ -141,6 +141,14 @@ func TestEntityRefSlice_RemoveIf(t *testing.T) {
 	assert.Equal(t, 5, filtered.Len())
 }
 
+func TestEntityRefSlice_RemoveIfAll(t *testing.T) {
+	got := generateTestEntityRefSlice()
+	got.RemoveIf(func(el EntityRef) bool {
+		return true
+	})
+	assert.Equal(t, 0, got.Len())
+}
+
 func TestEntityRefSliceAll(t *testing.T) {
 	ms := generateTestEntityRefSlice()
 	assert.NotEmpty(t, ms.Len())
