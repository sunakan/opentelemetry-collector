diff --git a/pdata/internal/generated_wrapper_stringslice.go b/pdata/internal/generated_wrapper_stringslice.go
index 856562a72..7ae773650 100644
--- a/pdata/internal/generated_wrapper_stringslice.go
+++ b/pdata/internal/generated_wrapper_stringslice.go
@@ -27,16 +27,10 @@ func NewStringSlice(orig *[]string, state *State) StringSlice {
 	return StringSlice{orig: orig, state: state}
 }
 
-func FillTestStringSlice(ms StringSlice) {
-	*ms.orig = []string{"a", "b", "c"}
-}
-
 func GenerateTestStringSlice() StringSlice {
-	orig := []string(nil)
+	orig := GenerateOrigTestStringSlice()
 	state := StateMutable
-	ms := NewStringSlice(&orig, &state)
-	FillTestStringSlice(ms)
-	return ms
+	return NewStringSlice(&orig, &state)
 }
 
 // MarshalJSONStreamStringSlice marshals all properties from the current struct to the destination stream.
@@ -63,3 +57,7 @@ func UnmarshalJSONIterStringSlice(ms StringSlice, iter *json.Iterator) {
 func CopyOrigStringSlice(dst, src []string) []string {
 	return append(dst[:0], src...)
 }
+
+func GenerateOrigTestStringSlice() []string {
+	return []string{"a", "b", "c"}
+}
