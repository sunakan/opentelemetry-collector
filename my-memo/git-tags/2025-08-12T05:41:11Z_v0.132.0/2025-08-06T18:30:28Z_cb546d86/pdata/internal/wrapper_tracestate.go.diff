diff --git a/pdata/internal/wrapper_tracestate.go b/pdata/internal/wrapper_tracestate.go
index fe0f9a0e2..81a60760a 100644
--- a/pdata/internal/wrapper_tracestate.go
+++ b/pdata/internal/wrapper_tracestate.go
@@ -24,20 +24,11 @@ func NewTraceState(orig *string, state *State) TraceState {
 	return TraceState{orig: orig, state: state}
 }
 
-func CopyOrigTraceState(dest, src *string) {
-	*dest = *src
-}
-
 func GenerateTestTraceState() TraceState {
 	var orig string
+	FillOrigTestTraceState(&orig)
 	state := StateMutable
-	ms := NewTraceState(&orig, &state)
-	FillTestTraceState(ms)
-	return ms
-}
-
-func FillTestTraceState(dest TraceState) {
-	*dest.orig = "rojo=00f067aa0ba902b7"
+	return NewTraceState(&orig, &state)
 }
 
 // MarshalJSONStreamTraceState marshals all properties from the current struct to the destination stream.
@@ -49,3 +40,11 @@ func MarshalJSONStreamTraceState(ms TraceState, dest *json.Stream) {
 func UnmarshalJSONIterTraceState(ms TraceState, iter *json.Iterator) {
 	*ms.orig = iter.ReadString()
 }
+
+func CopyOrigTraceState(dest, src *string) {
+	*dest = *src
+}
+
+func FillOrigTestTraceState(orig *string) {
+	*orig = "rojo=00f067aa0ba902b7"
+}
