diff --git a/extension/pprofextension/pprofextension.go b/extension/pprofextension/pprofextension.go
index c75c42f6e..3825dfc35 100644
--- a/extension/pprofextension/pprofextension.go
+++ b/extension/pprofextension/pprofextension.go
@@ -68,7 +68,7 @@ func (p *pprofExtension) Start(_ context.Context, host component.Host) error {
 	// Start the listener here so we can have earlier failure if port is
 	// already in use.
 	var ln net.Listener
-	ln, startErr = net.Listen("tcp", p.config.Endpoint)
+	ln, startErr = p.config.TCPAddr.Listen()
 	if startErr != nil {
 		return startErr
 	}
