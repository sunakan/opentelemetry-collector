diff --git a/examples/k8s.yaml b/examples/k8s.yaml
index 17b896fc4..b5f60fa6e 100644
--- a/examples/k8s.yaml
+++ b/examples/k8s.yaml
@@ -115,11 +115,11 @@ data:
       batch:
       memory_limiter:
         # Same as --mem-ballast-size-mib CLI argument
-        ballast_size_mib: 165
+        ballast_size_mib: 683
         # 80% of maximum memory up to 2G
-        limit_mib: 400
+        limit_mib: 1500
         # 25% of limit up to 2G
-        spike_limit_mib: 100
+        spike_limit_mib: 512
         check_interval: 5s
       queued_retry:
     extensions:
