diff --git a/cmd/otelcol/config.yaml b/cmd/otelcol/config.yaml
index ce9ba1a84..af6b1b440 100644
--- a/cmd/otelcol/config.yaml
+++ b/cmd/otelcol/config.yaml
@@ -13,15 +13,6 @@ receivers:
 
   opencensus:
 
-  # Collect own metrics
-  prometheus:
-    config:
-      scrape_configs:
-      - job_name: 'otel-collector'
-        scrape_interval: 10s
-        static_configs:
-        - targets: ['0.0.0.0:8888']
-
   jaeger:
     protocols:
       grpc:
@@ -48,7 +39,7 @@ service:
       exporters: [logging]
 
     metrics:
-      receivers: [otlp, opencensus, prometheus]
+      receivers: [otlp, opencensus]
       processors: [batch]
       exporters: [logging]
 
