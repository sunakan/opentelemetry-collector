diff --git a/examples/local/otel-config.yaml b/examples/local/otel-config.yaml
index d189f446c..e04e7c3c4 100644
--- a/examples/local/otel-config.yaml
+++ b/examples/local/otel-config.yaml
@@ -10,14 +10,6 @@ receivers:
     protocols:
       grpc:
       http:
-  opencensus:
-  jaeger:
-    protocols:
-      grpc:
-      thrift_binary:
-      thrift_compact:
-      thrift_http:
-  zipkin:
 
 processors:
   batch:
@@ -29,11 +21,11 @@ exporters:
 service:
   pipelines:
     traces:
-      receivers: [otlp, opencensus, jaeger, zipkin]
+      receivers: [otlp]
       processors: [batch]
       exporters: [logging]
     metrics:
-      receivers: [otlp, opencensus]
+      receivers: [otlp]
       processors: [batch]
       exporters: [logging]
 
