diff --git a/.circleci/config.yml b/.circleci/config.yml
index 5aa4326f2..bda359255 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -13,6 +13,7 @@ commands:
     parameters:
       files:
         type: string
+        # TODO: Add dist/otel-collector-*amd64.msi back
         default: |
             bin/otelcol_darwin_amd64
             bin/otelcol_linux_arm64
@@ -22,7 +23,6 @@ commands:
             dist/otel-collector_*amd64.deb
             dist/otel-collector-*x86_64.rpm
             dist/otel-collector_*arm64.deb
-            dist/otel-collector-*amd64.msi
     steps:
       - run:
           name: Check if files exist
@@ -145,12 +145,6 @@ workflows:
           filters:
             tags:
               only: /^v([0-9])+.([0-9])+.([0-9])+.*/
-      - windows-msi:
-          requires:
-            - cross-compile
-          filters:
-            tags:
-              only: /^v([0-9])+.([0-9])+.([0-9])+.*/
       - publish-check:
           requires:
             - cross-compile
@@ -161,7 +155,6 @@ workflows:
             - gosec
             - coverage
             - windows-test
-            - windows-msi
             - deb-package
             - rpm-package
       - publish-stable:
@@ -174,7 +167,6 @@ workflows:
             - gosec
             - coverage
             - windows-test
-            - windows-msi
             - deb-package
             - rpm-package
           filters:
@@ -192,7 +184,6 @@ workflows:
             - gosec
             - coverage
             - windows-test
-            - windows-msi
             - deb-package
             - rpm-package
           filters:
