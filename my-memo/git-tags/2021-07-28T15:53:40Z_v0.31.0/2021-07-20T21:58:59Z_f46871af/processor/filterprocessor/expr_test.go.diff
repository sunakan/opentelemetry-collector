diff --git a/processor/filterprocessor/expr_test.go b/processor/filterprocessor/expr_test.go
index 24afae6b2..b926ae46e 100644
--- a/processor/filterprocessor/expr_test.go
+++ b/processor/filterprocessor/expr_test.go
@@ -60,7 +60,6 @@ func TestExprProcessor(t *testing.T) {
 	testFilter(t, pdata.MetricDataTypeGauge)
 	testFilter(t, pdata.MetricDataTypeIntSum)
 	testFilter(t, pdata.MetricDataTypeSum)
-	testFilter(t, pdata.MetricDataTypeIntHistogram)
 	testFilter(t, pdata.MetricDataTypeHistogram)
 }
 
@@ -110,11 +109,6 @@ func testFilter(t *testing.T, mdType pdata.MetricDataType) {
 							for l := 0; l < pts.Len(); l++ {
 								assertFiltered(t, pts.At(l).LabelsMap())
 							}
-						case pdata.MetricDataTypeIntHistogram:
-							pts := metric.IntHistogram().DataPoints()
-							for l := 0; l < pts.Len(); l++ {
-								assertFiltered(t, pts.At(l).LabelsMap())
-							}
 						case pdata.MetricDataTypeHistogram:
 							pts := metric.Histogram().DataPoints()
 							for l := 0; l < pts.Len(); l++ {
