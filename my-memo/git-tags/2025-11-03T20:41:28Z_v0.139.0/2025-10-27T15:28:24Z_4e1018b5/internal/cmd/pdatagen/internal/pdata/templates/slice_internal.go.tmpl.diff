diff --git a/internal/cmd/pdatagen/internal/pdata/templates/slice_internal.go.tmpl b/internal/cmd/pdatagen/internal/pdata/templates/slice_internal.go.tmpl
index 9d274e023..3b48e0280 100644
--- a/internal/cmd/pdatagen/internal/pdata/templates/slice_internal.go.tmpl
+++ b/internal/cmd/pdatagen/internal/pdata/templates/slice_internal.go.tmpl
@@ -11,14 +11,13 @@ import (
 	{{ $element }}
 	{{ end }}
 )
-{{ if .hasWrapper }}
 
 type {{ .structName }}Wrapper struct {
-	orig *[]{{ if .elementNullable }}*{{ end }}{{ .elementOriginFullName }}
+	orig *[]{{ if .elementNullable }}*{{ end }}{{ .elementOriginName }}
 	state *State
 }
 
-func Get{{ .structName }}Orig(ms {{ .structName }}Wrapper) *[]{{ if .elementNullable }}*{{ end }}{{ .elementOriginFullName }} {
+func Get{{ .structName }}Orig(ms {{ .structName }}Wrapper) *[]{{ if .elementNullable }}*{{ end }}{{ .elementOriginName }} {
 	return ms.orig
 }
 
@@ -26,65 +25,11 @@ func Get{{ .structName }}State(ms {{ .structName }}Wrapper) *State {
 	return ms.state
 }
 
-func New{{ .structName }}Wrapper(orig *[]{{ if .elementNullable }}*{{ end }}{{ .elementOriginFullName }}, state *State) {{ .structName }}Wrapper {
+func New{{ .structName }}Wrapper(orig *[]{{ if .elementNullable }}*{{ end }}{{ .elementOriginName }}, state *State) {{ .structName }}Wrapper {
 	return {{ .structName }}Wrapper{orig: orig, state: state}
 }
 
 func GenTest{{ .structName }}Wrapper() {{ .structName }}Wrapper {
-	orig := GenTest{{ .elementOriginName }}Slice()
-	return New{{ .structName }}Wrapper(&orig, NewState())
-}
-{{ end }}
-
-func Copy{{ .elementOriginName }}Slice(dest, src []{{ if .elementNullable }}*{{ end }}{{ .elementOriginFullName }}) []{{ if .elementNullable }}*{{ end }}{{ .elementOriginFullName }} {
-	var newDest []{{ if .elementNullable }}*{{ end }}{{ .elementOriginFullName }}
-	if cap(dest) < len(src) {
-		{{ if .elementNullable -}}
-		newDest = make([]*{{ .elementOriginFullName }}, len(src))
-		// Copy old pointers to re-use.
-		copy(newDest, dest)
-		// Add new pointers for missing elements from len(dest) to len(srt).
-		for i := len(dest); i < len(src); i++ {
-			newDest[i] = New{{ .elementOriginName }}()
-		}{{ else -}}
-		newDest = make([]{{ .elementOriginFullName }}, len(src))
-        {{- end }}
-	} else {
-		newDest = dest[:len(src)]
-		// Cleanup the rest of the elements so GC can free the memory.
-		// This can happen when len(src) < len(dest) < cap(dest).
-		{{ if .elementNullable -}}
-		for i := len(src); i < len(dest); i++ {
-            Delete{{ .elementOriginName }}(dest[i], true)
-			dest[i] = nil
-		}
-		// Add new pointers for missing elements.
-		// This can happen when len(dest) < len(src) < cap(dest).
-		for i := len(dest); i < len(src); i++ {
-			newDest[i] = New{{ .elementOriginName }}()
-		}{{ else -}}
-		for i := len(src); i < len(dest); i++ {
-            Delete{{ .elementOriginName }}(&dest[i], false)
-		}
-        {{- end }}
-	}
-	for i := range src {
-	    Copy{{ .elementOriginName }}({{ if not .elementNullable }}&{{ end }}newDest[i], {{ if not .elementNullable }}&{{ end }}src[i])
-	}
-	return newDest
-}
-
-func GenTest{{ .elementOriginName }}Slice() []{{ if .elementNullable }}*{{ end }}{{ .elementOriginFullName }} {
-    {{ if .elementNullable -}}
-    orig := make([]*{{ .elementOriginFullName }}, 5)
-	orig[0] = New{{ .elementOriginName }}()
-	orig[1] = GenTest{{ .elementOriginName }}()
-    orig[2] = New{{ .elementOriginName }}()
-	orig[3] = GenTest{{ .elementOriginName }}()
-    orig[4] = New{{ .elementOriginName }}(){{ else -}}
-    orig := make([]{{ .elementOriginFullName }}, 5)
-    orig[1] = *GenTest{{ .elementOriginName }}()
-    orig[3] = *GenTest{{ .elementOriginName }}()
-    {{- end }}
-	return orig
+    orig := GenTest{{ .elementOriginName }}{{ if .elementNullable }}Ptr{{ end }}Slice()
+    return New{{ .structName }}Wrapper(&orig, NewState())
 }
