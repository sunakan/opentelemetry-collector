diff --git a/receiver/fluentforwardreceiver/heartbeat_test.go b/receiver/fluentforwardreceiver/heartbeat_test.go
index b3f260b1c..f92aff4df 100644
--- a/receiver/fluentforwardreceiver/heartbeat_test.go
+++ b/receiver/fluentforwardreceiver/heartbeat_test.go
@@ -41,7 +41,7 @@ func TestUDPHeartbeat(t *testing.T) {
 	require.Equal(t, 1, n)
 
 	buf := make([]byte, 1)
-	conn.SetReadDeadline(time.Now().Add(5 * time.Second))
+	require.NoError(t, conn.SetReadDeadline(time.Now().Add(5*time.Second)))
 	n, err = conn.Read(buf)
 	require.Nil(t, err)
 	require.Equal(t, 1, n)
