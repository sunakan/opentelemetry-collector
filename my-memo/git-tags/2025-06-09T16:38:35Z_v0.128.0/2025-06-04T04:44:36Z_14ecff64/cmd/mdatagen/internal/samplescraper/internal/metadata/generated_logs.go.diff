diff --git a/cmd/mdatagen/internal/samplescraper/internal/metadata/generated_logs.go b/cmd/mdatagen/internal/samplescraper/internal/metadata/generated_logs.go
index 93a699c37..0f936fce0 100644
--- a/cmd/mdatagen/internal/samplescraper/internal/metadata/generated_logs.go
+++ b/cmd/mdatagen/internal/samplescraper/internal/metadata/generated_logs.go
@@ -69,7 +69,7 @@ func (lb *LogsBuilder) AppendLogRecord(lr plog.LogRecord) {
 // just `Emit` function can be called instead.
 // Resource attributes should be provided as ResourceLogsOption arguments.
 func (lb *LogsBuilder) EmitForResource(options ...ResourceLogsOption) {
-	rl := lb.logsBuffer.ResourceLogs().AppendEmpty()
+	rl := plog.NewResourceLogs()
 	rl.SetSchemaUrl(conventions.SchemaURL)
 	ils := rl.ScopeLogs().AppendEmpty()
 	ils.Scope().SetName(ScopeName)
@@ -83,6 +83,10 @@ func (lb *LogsBuilder) EmitForResource(options ...ResourceLogsOption) {
 		lb.logRecordsBuffer.MoveAndAppendTo(ils.LogRecords())
 		lb.logRecordsBuffer = plog.NewLogRecordSlice()
 	}
+
+	if ils.LogRecords().Len() > 0 {
+		rl.MoveTo(lb.logsBuffer.ResourceLogs().AppendEmpty())
+	}
 }
 
 // Emit returns all the logs accumulated by the logs builder and updates the internal state to be ready for
