diff --git a/receiver/prometheusreceiver/testdata/config_k8s.yaml b/receiver/prometheusreceiver/testdata/config_k8s.yaml
index c73ed6eee..56c5bb1ef 100644
--- a/receiver/prometheusreceiver/testdata/config_k8s.yaml
+++ b/receiver/prometheusreceiver/testdata/config_k8s.yaml
@@ -29,14 +29,14 @@ receivers:
           target_label: kubernetes_pod_name
 
 processors:
-  exampleprocessor:
+  nop:
 
 exporters:
-  exampleexporter:
+  nop:
 
 service:
   pipelines:
     traces:
       receivers: [prometheus]
-      processors: [exampleprocessor]
-      exporters: [exampleexporter]
+      processors: [nop]
+      exporters: [nop]
