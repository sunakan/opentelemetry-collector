diff --git a/receiver/fluentforwardreceiver/testdata/config.yaml b/receiver/fluentforwardreceiver/testdata/config.yaml
index 863f73db8..06a7ad62d 100644
--- a/receiver/fluentforwardreceiver/testdata/config.yaml
+++ b/receiver/fluentforwardreceiver/testdata/config.yaml
@@ -2,14 +2,14 @@ receivers:
   fluentforward:
 
 processors:
-  exampleprocessor:
+  nop:
 
 exporters:
-  exampleexporter:
+  nop:
 
 service:
   pipelines:
     logs:
       receivers: [fluentforward]
-      processors: [exampleprocessor]
-      exporters: [exampleexporter]
+      processors: [nop]
+      exporters: [nop]
