diff --git a/exporter/exporterbatcher/sizer_type_test.go b/exporter/exporterbatcher/sizer_type_test.go
index f5dc14816..26b0464c0 100644
--- a/exporter/exporterbatcher/sizer_type_test.go
+++ b/exporter/exporterbatcher/sizer_type_test.go
@@ -6,26 +6,28 @@ package exporterbatcher
 import (
 	"testing"
 
+	"github.com/stretchr/testify/assert"
 	"github.com/stretchr/testify/require"
-	"gopkg.in/yaml.v2"
-
-	"go.opentelemetry.io/collector/confmap"
 )
 
-func TestSizeTypeUnmarshaler(t *testing.T) {
-	var rawConf map[string]any
-	cfg := NewDefaultConfig()
+func TestSizeTypeUnmarshalText(t *testing.T) {
+	var sizer SizerType
+	require.NoError(t, sizer.UnmarshalText([]byte("bytes")))
+	require.NoError(t, sizer.UnmarshalText([]byte("items")))
+	require.NoError(t, sizer.UnmarshalText([]byte("requests")))
+	require.Error(t, sizer.UnmarshalText([]byte("invalid")))
+}
 
-	require.NoError(t, yaml.Unmarshal([]byte(`sizer: items`), &rawConf))
-	require.NoError(t, confmap.NewFromStringMap(rawConf).Unmarshal(&cfg))
-	require.NoError(t, cfg.Validate())
+func TestSizeTypeMarshalText(t *testing.T) {
+	val, err := SizerTypeBytes.MarshalText()
+	require.NoError(t, err)
+	assert.Equal(t, []byte("bytes"), val)
 
-	require.NoError(t, yaml.Unmarshal([]byte(`sizer: 'items'`), &rawConf))
-	require.NoError(t, confmap.NewFromStringMap(rawConf).Unmarshal(&cfg))
-	require.NoError(t, cfg.Validate())
+	val, err = SizerTypeItems.MarshalText()
+	require.NoError(t, err)
+	assert.Equal(t, []byte("items"), val)
 
-	require.NoError(t, yaml.Unmarshal([]byte(`sizer: invalid`), &rawConf))
-	require.ErrorContains(t,
-		confmap.NewFromStringMap(rawConf).Unmarshal(&cfg),
-		"decoding failed due to the following error(s):\n\nerror decoding 'sizer': invalid sizer: \"invalid\"")
+	val, err = SizerTypeRequests.MarshalText()
+	require.NoError(t, err)
+	assert.Equal(t, []byte("requests"), val)
 }
