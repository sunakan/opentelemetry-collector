diff --git a/pdata/pcommon/generated_float64slice_test.go b/pdata/pcommon/generated_float64slice_test.go
index ec95a3c0f..ac6a93bbe 100644
--- a/pdata/pcommon/generated_float64slice_test.go
+++ b/pdata/pcommon/generated_float64slice_test.go
@@ -82,6 +82,19 @@ func TestFloat64SliceEnsureCapacity(t *testing.T) {
 	assert.Equal(t, 4, cap(*ms.getOrig()))
 }
 
+func TestFloat64SliceAll(t *testing.T) {
+	ms := NewFloat64Slice()
+	ms.FromRaw([]float64{1, 2, 3})
+	assert.NotEmpty(t, ms.Len())
+
+	var c int
+	for i, v := range ms.All() {
+		assert.Equal(t, ms.At(i), v, "element should match")
+		c++
+	}
+	assert.Equal(t, ms.Len(), c, "All elements should have been visited")
+}
+
 func TestFloat64SliceEqual(t *testing.T) {
 	ms := NewFloat64Slice()
 	ms2 := NewFloat64Slice()
