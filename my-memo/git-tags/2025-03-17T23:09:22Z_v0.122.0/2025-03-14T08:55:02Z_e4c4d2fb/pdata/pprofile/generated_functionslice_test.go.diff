diff --git a/pdata/pprofile/generated_functionslice_test.go b/pdata/pprofile/generated_functionslice_test.go
index 1fd9fd46b..612ef0b37 100644
--- a/pdata/pprofile/generated_functionslice_test.go
+++ b/pdata/pprofile/generated_functionslice_test.go
@@ -123,6 +123,18 @@ func TestFunctionSlice_RemoveIf(t *testing.T) {
 	assert.Equal(t, 5, filtered.Len())
 }
 
+func TestFunctionSliceAll(t *testing.T) {
+	ms := generateTestFunctionSlice()
+	assert.NotEmpty(t, ms.Len())
+
+	var c int
+	for i, v := range ms.All() {
+		assert.Equal(t, ms.At(i), v, "element should match")
+		c++
+	}
+	assert.Equal(t, ms.Len(), c, "All elements should have been visited")
+}
+
 func TestFunctionSlice_Sort(t *testing.T) {
 	es := generateTestFunctionSlice()
 	es.Sort(func(a, b Function) bool {
