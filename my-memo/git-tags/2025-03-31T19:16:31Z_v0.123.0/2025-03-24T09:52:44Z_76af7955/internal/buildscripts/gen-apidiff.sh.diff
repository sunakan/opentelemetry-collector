diff --git a/internal/buildscripts/gen-apidiff.sh b/internal/buildscripts/gen-apidiff.sh
index d51815807..3cbbc1946 100755
--- a/internal/buildscripts/gen-apidiff.sh
+++ b/internal/buildscripts/gen-apidiff.sh
@@ -45,7 +45,8 @@ fi
 set -ex
 
 # Create temp dir for generated files.
-tmp_dir=$(mktemp -d -t apidiff)
+# Source: https://unix.stackexchange.com/a/84980
+tmp_dir=$(mktemp -d 2>/dev/null || mktemp -d -t 'apidiff')
 clean_up() {
     ARG=$?
     if [ $dry_run = true ]; then
