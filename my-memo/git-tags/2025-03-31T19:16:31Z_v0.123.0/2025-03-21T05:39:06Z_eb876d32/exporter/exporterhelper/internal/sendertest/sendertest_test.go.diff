diff --git a/exporter/exporterhelper/internal/sendertest/sendertest_test.go b/exporter/exporterhelper/internal/sendertest/sendertest_test.go
new file mode 100644
index 000000000..400c8de69
--- /dev/null
+++ b/exporter/exporterhelper/internal/sendertest/sendertest_test.go
@@ -0,0 +1,23 @@
+// Copyright The OpenTelemetry Authors
+// SPDX-License-Identifier: Apache-2.0
+
+package sendertest
+
+import (
+	"context"
+	"errors"
+	"testing"
+
+	"github.com/stretchr/testify/require"
+)
+
+func TestNewNopSenderFunc(t *testing.T) {
+	sender := NewNopSenderFunc[int]()
+	require.NoError(t, sender(context.Background(), 1))
+}
+
+func TestNewErrSenderFunc(t *testing.T) {
+	err := errors.New("test")
+	sender := NewErrSenderFunc[int](err)
+	require.ErrorIs(t, sender(context.Background(), 1), err)
+}
