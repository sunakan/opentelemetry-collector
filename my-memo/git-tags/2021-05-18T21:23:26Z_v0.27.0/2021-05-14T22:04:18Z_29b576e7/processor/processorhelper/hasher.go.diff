diff --git a/processor/processorhelper/hasher.go b/processor/processorhelper/hasher.go
index 6f0999a95..aad2cfe55 100644
--- a/processor/processorhelper/hasher.go
+++ b/processor/processorhelper/hasher.go
@@ -61,7 +61,7 @@ func sha1Hasher(attr pdata.AttributeValue) {
 	if len(val) > 0 {
 		// #nosec
 		h := sha1.New()
-		h.Write(val)
+		h.Write(val) // nolint: errcheck
 		val = h.Sum(nil)
 		hashedBytes := make([]byte, hex.EncodedLen(len(val)))
 		hex.Encode(hashedBytes, val)
